<div class="form-group nested-fields">
  <%= f.label :name %>
  <%= f.text_field :name, class: 'form-control' %>
  <%= link_to_remove_association 'Remove Technology', f %> <!--This is linked to the allow_destroy :true in the model as well as the :_destroy in params in the controller. -->
</div>

<!-- nested-fields in the class name is also required for the destroy task to work according tto the cooon documentation -->